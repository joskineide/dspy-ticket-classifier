# LM mode: "direct" (Ollama) or "gateway" (AI Gateway)
LM_MODE=direct

# --- Option A: direct Ollama ---
OLLAMA_API_BASE=http://localhost:11434

# --- Option B: AI Gateway ---
# GATEWAY_URL=http://localhost:8000/v1
# GATEWAY_API_KEY=sk-your-gateway-key-here

# Model used for classification (LiteLLM provider prefix)
CLASSIFIER_MODEL=ollama/llama3.1
